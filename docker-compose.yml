services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: sunbuild
      POSTGRES_PASSWORD: sunbuild
      POSTGRES_DB: sunbuild
    ports:
      - "5432:5432"
    volumes:
      - sunbuild_pg:/var/lib/postgresql/data

volumes:
  sunbuild_pg:
